Работай в C:\projects\HomeworkParser. Реализуй план из docs/luna-remediation-plan.md на основании security_best_practices_report.md.

Передача этого задания означает моё утверждение решений, перечисленных в плане: один worker и SQLite, сопоставление полного набора задач, транзакционное сохранение снимка с очередью на существующих полях, отдельная доставка, безопасная миграция дат, одно сокращённое уведомление со ссылкой и ограниченный refresh существующего Classroom web/RPC. Для этих решений не запрашивай повторное разрешение. Существенный выход за этот объём сначала объясни и согласуй.

КРИТИЧЕСКОЕ УТОЧНЕНИЕ: официальный Classroom API заблокирован, доступа к нему нет. Мы намеренно используем браузерные cookies и внутренний RPC. Оставь этот способ. Не переходи на OAuth/официальный API, не добавляй такой fallback, не требуй разрешений администратора и не переписывай рабочие запросы ради чистоты архитектуры. Исправляй конкретные сбои используемого web-пути, сохраняя его рабочие wire-shapes, маски и cookie-семантику. School/student id и дату отсечения оставь текущими: универсализация для других аккаунтов сейчас не нужна. Это уточнение важнее рекомендаций исторического отчёта.

Сначала прочитай AGENTS.md, docs/ARCHITECTURE.md, README.md, package.json, отчёт и весь план. Используй brainstorming для сверки утверждённого дизайна и security-best-practices для исправлений безопасности. План уже дан; не ограничивайся повторным аудитом или обещанием выполнить работу.

Выполняй этапы последовательно:
0. Исходные тесты и карта finding → regression test → исправление.
1. Утечки cookies/JSON/ошибок и неопределённый index.
2. Совместимый поддерживаемый Node runtime и чистая проверка сборки.
3. Однозначное сопоставление E-school без слияния разных заданий.
4. Транзакционное обновление снимка отдельно от устойчивой очереди доставки.
5. ISO-даты и безопасная повторяемая миграция старых дат.
6. Уведомления, укладывающиеся в лимит Telegram без повреждения HTML.
7. Полные тайм-ауты, независимый запуск провайдеров, отмена и graceful shutdown.
8. Ограниченное восстановление существующего Classroom web/RPC и сохранность данных при непонятном ответе. Не менять рабочий способ доступа, текущие ids/cutoff и поддерживаемые форматы ответа.
9. Полный прогон на runtime Render, документация и отчёт по каждому finding.

Каждый дефект сначала воспроизведи регрессионным тестом желаемого поведения. output/audit-probes.mjs содержит диагностические примеры текущих ошибок, а не готовые тесты исправленной версии. Не ослабляй assertions и не заменяй рабочую реализацию fixture-ответами. Проверяй содержимое БД, id, статусы, очередь и количество сообщений, а не только отсутствие исключений.

Используй существующие auth/API/Telegram-адаптеры и ESM-стиль. Ручные изменения делай apply_patch. Не выполняй несвязанный рефакторинг, не добавляй новую инфраструктуру и не обновляй все зависимости без причины. Для schema/data migration соблюдай DATABASE_VERSION и сохранность данных.

Не выводи .env, пароли, cookies, токены, OAuth URLs и личные задания. Не открывай и не изменяй пользовательскую SQLite ради тестов. Используй :memory: или временные базы и искусственные секреты. Не удаляй data/, state или каталог проекта. Не меняй окружение работающего бота; чистые установки выполняй в отдельной временной копии без приватных файлов.

Это разрешение на локальные изменения кода, тестов и документации. Не запускай npm start, npm run sync, npm run bot, npm run telegram:test, classroom smoke/auth; не отправляй сообщения, не перезапускай существующий бот, не выполняй деплой, push, перенос базы или ротацию секретов. Эти реальные действия требуют отдельного прямого разрешения.

Не угадывай внутренний RPC Google. Если полноту Classroom нельзя доказать локально, исправь доступные механизмы восстановления/валидации и явно оставь полноту непроверенной с конкретным планом live-эксперимента именно через web/RPC. Не отключай из-за этого рабочий импорт, не блокируй независимые исправления и не заменяй задачу предложением подключить недоступный официальный API. Исправление timeout его неиспользуемого SDK из F6 отложи и честно укажи это в итоговом отчёте.

После каждого этапа запускай релевантные тесты. Перед сдачей — весь npm test на выбранной версии Node, npm audit --omit=dev и безопасная проверка чистой сборки. Сверяй Node/Render параметры с официальной документацией. Сохраняй прогресс в docs/remediation-results.md, чтобы продолжить после смены контекста.

В финале сообщи: какие findings F1–F10 исправлены, какие проверки это доказывают, точную Node-версию, изменённые файлы, результаты тестов/audit, ограничения Classroom и оставшиеся действия для реального деплоя. Отличай «прошло offline-тесты» от «проверено с живыми сервисами». Не объявляй работу завершённой только потому, что прежние 102 теста зелёные.
